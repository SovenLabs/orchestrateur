{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Capabilities par défaut — Orchestrateur desktop (Phases 21–25)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}