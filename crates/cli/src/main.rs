//! Binaire principal — Phase 5 apportera assimilate, search, chat, graph.

use anyhow::Result;

fn main() -> Result<()> {
    println!(
        "Orchestrateur v{} — Phase 1 clôturée (Cortex 0.1.0). Phase 2 : Esprit à venir.",
        env!("CARGO_PKG_VERSION")
    );
    Ok(())
}