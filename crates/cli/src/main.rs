//! Binaire principal — Phase 5 apportera assimilate, search, chat, graph.

use anyhow::Result;

fn main() -> Result<()> {
    println!(
        "Orchestrateur v{} — Phase 2 clôturée (Cortex + Esprit 0.1.0). Phase 3 : Infrastructure à venir.",
        env!("CARGO_PKG_VERSION")
    );
    Ok(())
}