---
name: cortex-capture
description: Capture une idée dans le Cortex via assimilation structurée.
---

# cortex-capture

Utilise les outils harness pour persister dans le Cortex :

1. Classifier le kind (`insight`, `decision`, `pattern`, …)
2. Appeler `memory_assimilate` ou `cortex_assimilate` (MCP)
3. Ne jamais écrire de fichier mémoire manuellement — le Cortex est la source de vérité

Tags suggérés : `capture`, `harness`.